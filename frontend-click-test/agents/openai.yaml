interface:
  display_name: "前端点击测试"
  short_description: "前端测试失败时自动收集证据、定位修复并循环验证，直到通过"
  default_prompt: "使用 $frontend-click-test 测试刚完成的前端功能；失败时自动定位、最小修复并循环验证，直到通过或触发停止条件。"
policy:
  allow_implicit_invocation: true
